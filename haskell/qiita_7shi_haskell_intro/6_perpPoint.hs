-- original     : ax + by = c
-- perpendicular: bx - ay = d

perpPoint (p, q) (a, b, c) = 
d = b*x - a*y

ax + by = c
     by = -ax + c
      y = -(a/b)*x + c/b

      y' = b/a*x' + n
      y' = (a/b)*x' + c/b
