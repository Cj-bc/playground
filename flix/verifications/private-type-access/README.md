# private-type-in-piblic-type

"Sealed" datatype in `Sealed` module is declared as private. however, it seems like it is accessible from other modules(`Another`/`Main`).
this project compiles fine.  

I'm quite not sure why... am I misunderstanding something?